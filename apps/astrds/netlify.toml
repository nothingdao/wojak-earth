[build]
command = "pnpm --filter solana-asteroids build"
publish = "dist"

[build.environment]
NODE_VERSION = "22"
PNPM_VERSION = "9.0.0"

[dev]
command = "pnpm dev"
targetPort = 3000
autoLaunch = false
